# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# AI Provider: OpenAI (GPT-4o, GPT-4 Turbo, o1 models)
# Replaces Anthropic Claude (see Task 009 for migration)
OPENAI_API_KEY=your_openai_api_key

# Optional: Override default model (default: gpt-4o-2024-11-20)
# OPENAI_MODEL=gpt-4o-2024-11-20        # Recommended: Fast, cost-effective, multimodal
# OPENAI_MODEL=gpt-4-turbo-preview      # Alternative: Higher quality, more expensive
# OPENAI_MODEL=o1-preview               # Reasoning-focused, slowest, most expensive

# Prompt Separation (Future - Phase 2 of Tasks 001-004)
# Uncomment to enable prompt management via AI provider
# Leave commented to use local template files (recommended)
# PROMPT_MANAGEMENT_ENABLED=false
# PROMPT_SOURCE=local                   # Options: local, openai, anthropic

# App URL (optional)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin Access (comma-separated list of admin emails)
ADMIN_EMAILS=admin@example.com,another-admin@example.com
